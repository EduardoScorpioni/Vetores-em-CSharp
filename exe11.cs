using System;

class exe11
{
    /*
    static void Main()
    {
        Console.Write("Quantos números deseja inserir? ");
        int n = Convert.ToInt32(Console.ReadLine());

        int[] numeros = new int[n];
        int[] resultadoQuadrado = new int[n];
        int[] resultadoCubo = new int[n];

        for (int i = 0; i < n; i++)
        {
            Console.Write("Digite os numeros");
            numeros[i] = Convert.ToInt32(Console.ReadLine());

            resultadoQuadrado[i] = numeros[i] * numeros[i];
            resultadoCubo[i]     = numeros[i] * numeros[i] * numeros[i];
        }

        for (int i = 0; i < n; i++)
        {
           Console.WriteLine("o resultado do vetor ao cubo " + resultadoCubo[i]);
        }

       
        for (int i = 0; i < n; i++)
        {
            Console.WriteLine("o resultado do vetor quadrado " + resultadoQuadrado[i]);
        }
    } */
}
using System;
using System.Runtime.InteropServices;

 class exe8
    { /*
        
        static void Main(string[] args)
    {
                int i, n,constante;

               Console.WriteLine ("digite o tamanho do vetor: ");
               n = Convert.ToInt32(Console.ReadLine());

                int[]vetor = new int[n];

                Console.WriteLine ("digite um numero constante : ");
                constante = Convert.ToInt32(Console.ReadLine());
                
                for(i = 0; i < n; i++)
                {
                    Console.WriteLine("digite os valores do vetor");
                    vetor[i] = Convert.ToInt32(Console.ReadLine());
                }

               int multi;
              

               for(i = 0; i<n; i++)
                { 
                       multi = constante * vetor[i];
                       vetor[i] = multi;
                        
                }
               for(i = 0; i<n; i++)
                { 
                    Console.WriteLine("multiplicação dos numeros reais: " + vetor[i]);
                }
                
    }  */
    }
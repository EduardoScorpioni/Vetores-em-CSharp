using System;
using System.Runtime.InteropServices;

class exe7
    { 
        /*
        
        static void Main(string[] args)
    {
                int i, n;

               Console.WriteLine ("digite o tamanho do vetor: ");
               n = Convert.ToInt32(Console.ReadLine());

                int[] vetor = new int[n];
                
                for(i = 0; i < n; i++)
                {

                    Console.WriteLine("digite os numeros: ");
                    vetor[i] = Convert.ToInt32(Console.ReadLine());
                }
               int soma = 0;
               int multi= 1;

               for(i = 0; i<n; i++)
                {
                    if (vetor[i] % 2 == 0)
                    {   
                        soma = soma + vetor[i];
                        multi = multi * vetor[i];
                        
                    }
                }
                Console.WriteLine("soma pares: " + soma);
                Console.WriteLine("multiplicação dos pares: "+ multi);

                
    } */
    }
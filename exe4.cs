using System;
using System.Runtime.InteropServices;

class exe4
    {
        /*
        
        static void Main(string[] args)
    {
                int i, maior1,maior2, n;

               Console.WriteLine ("digite o tamanho do vetor: ");
               n = Convert.ToInt32(Console.ReadLine());

                int[] vetor = new int[n];
                
                for(i = 0; i < n; i++)
                {
                    Console.WriteLine("digite os nuemeros: ");
                    vetor[i] = Convert.ToInt32(Console.ReadLine());
                }

                maior1 = -998;
                maior2 = -999;
                int pos1 = 0;
                int pos2 = 0;
                
                for( i = 0; i <vetor.Length; i++)
                {
                    if(vetor[i] > maior1)
                    {
                        maior2 = maior1;
                        pos2 = pos1;
                        maior1 = vetor[i];
                        pos1 = i;

                    }
                    else if (vetor[i] > maior2)
                    {
                        maior2 = vetor[i];
                        pos2 = i;
                    }
            
                }

                Console.WriteLine("1 valor maior " +maior1+ " na posição "+ pos1);
                Console.WriteLine("2 valor maior " +maior2+ " na posição "+ pos2);
    } */ 
    }